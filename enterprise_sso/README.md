# enterprise_sso

Enterprise SSO/SAML readiness module for identity provider connections, verified domains, SSO policies, JIT provisioning controls, SAML metadata, and enterprise login event tracking.
